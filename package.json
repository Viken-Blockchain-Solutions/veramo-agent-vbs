{
  "name": "vbs-veramo-agent",
  "version": "1.0.0",
  "description": "General purpose Veramo Agent configuration deployments",
  "repository": "https://github.com/Viken-Blockchain-Solutions/veramo-agent-vbs",
  "license": "Apache-2.0",
  "scripts": {
    "start": "NODE_ENV=production yarn veramo server --config=./config/agent.yml",
    "dev": "yarn veramo server --config=./config/agent-local.yml"
  },
  "dependencies": {
    "@veramo/cli": "^5.5.3",
    "agent-explore": "^1.64.1",
    "dotenv": "^16.3.1"
  }
}
